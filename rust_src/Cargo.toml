[package]
name = "rustpart"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
path = "src/lib.rs"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
debug = false

[profile.dev]
panic = "abort"
opt-level = 0
debug = true
